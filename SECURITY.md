# Security Policy

## Reporting a vulnerability

برای گزارش خصوصی آسیب‌پذیری، از بخش Security Advisories همین مخزن استفاده کنید و جزئیات بازتولید، نسخهٔ لانچر و اثر احتمالی را بنویسید. گزارش‌های امنیتی را ابتدا به‌صورت Issue عمومی منتشر نکنید.

## Security model

- رمز عبور ذخیره‌شده در ویندوز با DPAPI و حساب همان کاربر محافظت می‌شود.
- بسته‌های بازی می‌توانند SHA-256 داشته باشند؛ مقدار هش باید از مسیر مورد اعتماد سرور دریافت شود.
- نام فایل‌های ZIP پیش از استخراج برای مسیر مطلق و `..` بررسی می‌شود.
- حذف پوشه فقط وقتی انجام می‌شود که `.irautox-install.json` شناسهٔ همان بازی را داشته باشد.
- لانچر بدون دسترسی Administrator اجرا و نصب می‌شود؛ کاربر می‌تواند هنگام Setup محل نصب را تغییر دهد.

